 general {
     ignore_dbus_inhibit = false
 }

listener {
    # sleep mode
    # timeout = 60
    # on-timeout = hyprctl dispatch dpms off  # command to run when timeout has passed
    # on-resume = hyprctl dispatch dpms on  # command to run when timeout has passed

    # day mode
    timeout = 600
    on-timeout = hyprlock
    on-resume = notify-send "Welcome Back!"
}

